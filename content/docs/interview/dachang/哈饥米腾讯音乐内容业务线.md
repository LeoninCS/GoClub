---
title: "哈饥米腾讯音乐内容业务线"
---

# 腾讯音乐内容业务线

## 存储与同步

1. 数据从 MySQL 同步到 ES 的方法。
2. binlog 设计之初的意义。

## 缓存与事务

1. Cache Aside 存在的问题，怎么解决？延迟双删。
2. 项目中的分布式事务问题，怎么解决？

## Go 与 Java

1. 对比 Go 和 Java 的 Map。
2. 为什么 Go 需要红黑树？Go 怎么防止哈希攻击？
3. 对比 Go 和 Java 的 GC。
4. 混合写屏障是怎么感知到引用的新增和接触的？
5. 对比 Go 和 Java 的协程/线程。

## MySQL

1. MySQL 聚簇索引和非聚簇索引，为什么非聚簇叶子节点中存在全部记录？
2. B+ 树结构，如果是联合索引 abc，每个非叶子节点的结构什么样？
3. Explain 分析 SQL，哪些字段要重点看？

## ES

1. ES 有三个数据节点，某个索引有三个分片，在不同的三个节点上。同一个索引存放的是一些纯数字的 ID，比较简单。如果去 ES 倒叙查 Top100，这个请求发给 ES 后，大概的简单实现原理是什么？基于你的基础知识分析下。

## 框架与中间件

1. Spring 怎么解决循环依赖？
2. Kafka 什么情况下会 rebalance？

## 手撕

1. 122。

## 参考答案（AI 生成）

> 以下答案由 AI 生成，仅供面试复盘参考。

### 1. 数据从 MySQL 同步到 ES 的方法。

答：MySQL 回答要围绕表结构、索引、事务、锁、日志和执行计划展开。场景题优先说明查询路径、并发风险和可观测指标。

### 2. binlog 设计之初的意义。

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 3. Cache Aside 存在的问题，怎么解决？延迟双删。

答：游戏服务端题重点是状态同步、命中判定、延迟补偿、分区分服和反作弊。实时体验通常由客户端预测提升流畅度，服务器做权威校验。

### 4. 项目中的分布式事务问题，怎么解决？

答：MySQL 回答要围绕表结构、索引、事务、锁、日志和执行计划展开。场景题优先说明查询路径、并发风险和可观测指标。

### 5. 对比 Go 和 Java 的 Map。

答：Go 题优先围绕 goroutine、channel、GMP、GC、内存逃逸和标准库工程能力展开，并结合项目里的并发场景说明。

### 6. 为什么 Go 需要红黑树？Go 怎么防止哈希攻击？

答：Go 题优先围绕 goroutine、channel、GMP、GC、内存逃逸和标准库工程能力展开，并结合项目里的并发场景说明。

### 7. 对比 Go 和 Java 的 GC。

答：Go GC 采用并发标记清扫，配合三色标记和写屏障降低停顿。STW 主要出现在标记准备、标记终止等短阶段。

### 8. 混合写屏障是怎么感知到引用的新增和接触的？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 9. 对比 Go 和 Java 的协程/线程。

答：进程是资源分配单位，线程是 CPU 调度单位，协程是用户态轻量执行单元。进程隔离强，线程共享进程地址空间，协程切换成本更低。

### 10. MySQL 聚簇索引和非聚簇索引，为什么非聚簇叶子节点中存在全部记录？

答：InnoDB 主键索引是聚簇索引，叶子节点保存整行；二级索引叶子节点保存主键值，查询其他列需要回表。覆盖索引能直接从索引返回结果。

### 11. B+ 树结构，如果是联合索引 abc，每个非叶子节点的结构什么样？

答：InnoDB 主键索引是聚簇索引，叶子节点保存整行；二级索引叶子节点保存主键值，查询其他列需要回表。覆盖索引能直接从索引返回结果。

### 12. Explain 分析 SQL，哪些字段要重点看？

答：看 EXPLAIN 时重点关注 type、key、rows、filtered、Extra。type 越接近 const/ref/range 越好，rows 越小越好，Extra 中关注 Using index、Using temporary、Using filesort。

### 13. ES 有三个数据节点，某个索引有三个分片，在不同的三个节点上。同一个索引存放的是一些纯数字的 ID，比较简单。如果去 ES 倒叙查 Top100，这个请求发给 ES 后，大概的简单实现原理是什么？基于你的基础知识分析下。

答：InnoDB 主键索引是聚簇索引，叶子节点保存整行；二级索引叶子节点保存主键值，查询其他列需要回表。覆盖索引能直接从索引返回结果。

### 14. Spring 怎么解决循环依赖？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 15. Kafka 什么情况下会 rebalance？

答：消息队列主要解决异步解耦、削峰填谷、失败重试和跨服务通信。面试回答要覆盖可靠投递、重复消费、积压处理和幂等。

### 16. 122。

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。
