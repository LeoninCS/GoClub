---
title: "哈饥米百度psig国际化 ai一面"
---

# 百度 psig 国际化 ai 一面

## 项目

1. 项目拷打。

## Redis

1. Redis 存一个商品的各项信息，用什么数据结构？
2. Redis 存一个商品的库存，可能并发增减，用什么数据结构？
3. Redis 存最近 20 条评论，用什么数据结构？除了 zset 还能用什么？
4. 缓存三剑客。
5. Redis 内存淘汰策略有什么？
6. Redis 事务和 MySQL 事务区别。

## MySQL

1. 怎么排查 MySQL 慢查询？
2. 假设用户表数据量太大，怎么分表？有没有后续动态去扩容这个表，同时它的分布也比较均匀的分表方法？
3. 用户表存了手机号，分表后一个一个表扫描太慢了，应该怎么查询？

## 计算机基础

1. 发送一个 curl 请求发生了什么？
2. 如何解析 SSE 的数据帧？
3. Linux I/O 多路复用原理？epoll 如果来了新事件，怎么唤醒阻塞进程？
4. Linux 权限 644 是什么？
5. Work stealing。

## 手撕

1. 多协程交替打印 1 和 2。
2. 堆排序。

## 参考答案（AI 生成）

> 以下答案由 AI 生成，仅供面试复盘参考。

### 1. 项目拷打。

答：项目回答按“背景、目标、架构、核心链路、难点、个人贡献、结果”组织。每个亮点都要能落到代码、数据结构或故障处理方案。

### 2. Redis 存一个商品的各项信息，用什么数据结构？

答：Redis 常见结构有 String、Hash、List、Set、ZSet。String 做缓存和计数，Hash 存对象，List 做队列，Set 做去重，ZSet 做排行榜和延迟队列。

### 3. Redis 存一个商品的库存，可能并发增减，用什么数据结构？

答：Redis 常见结构有 String、Hash、List、Set、ZSet。String 做缓存和计数，Hash 存对象，List 做队列，Set 做去重，ZSet 做排行榜和延迟队列。

### 4. Redis 存最近 20 条评论，用什么数据结构？除了 zset 还能用什么？

答：ZSet 常见实现是“字典 + 跳表”。字典按 member 快速定位 score，跳表按 score 维护有序集合，支持范围查询、排名和排行榜。小数据量时 Redis 会使用 listpack 节省内存。

### 5. 缓存三剑客。

答：缓存设计要考虑命中率、一致性、过期策略、击穿、雪崩和淘汰。多级缓存通常用本地缓存抗热点，用 Redis 做跨实例共享。

### 6. Redis 内存淘汰策略有什么？

答：Redis 适合热点缓存、计数、排行榜、分布式锁和队列辅助。回答时结合数据结构、过期策略、持久化、高可用和缓存一致性展开。

### 7. Redis 事务和 MySQL 事务区别。

答：Redis 适合热点缓存、计数、排行榜、分布式锁和队列辅助。回答时结合数据结构、过期策略、持久化、高可用和缓存一致性展开。

### 8. 怎么排查 MySQL 慢查询？

答：先用慢查询日志定位 SQL，再用 `EXPLAIN` 看 type、key、rows、Extra，确认索引命中、回表、临时表和 filesort。随后结合业务基数优化索引、改写 SQL、减少返回列，必要时做分页、缓存或异步化。

### 9. 假设用户表数据量太大，怎么分表？有没有后续动态去扩容这个表，同时它的分布也比较均匀的分表方法？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 10. 用户表存了手机号，分表后一个一个表扫描太慢了，应该怎么查询？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 11. 发送一个 curl 请求发生了什么？

答：浏览器先做 DNS 解析，再建立 TCP/TLS 连接，发送 HTTP 请求，服务端返回资源，浏览器解析 HTML、CSS、JS，构建 DOM/CSSOM 并完成布局绘制。

### 12. 如何解析 SSE 的数据帧？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 13. Linux I/O 多路复用原理？epoll 如果来了新事件，怎么唤醒阻塞进程？

答：进程是资源分配单位，线程是 CPU 调度单位，协程是用户态轻量执行单元。进程隔离强，线程共享进程地址空间，协程切换成本更低。

### 14. Linux 权限 644 是什么？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 15. Work stealing。

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 16. 多协程交替打印 1 和 2。

答：进程是资源分配单位，线程是 CPU 调度单位，协程是用户态轻量执行单元。进程隔离强，线程共享进程地址空间，协程切换成本更低。

### 17. 堆排序。

答：算法题先讲暴力思路，再给优化数据结构和复杂度。写代码时注意边界、空输入、重复元素和复杂度分析。
