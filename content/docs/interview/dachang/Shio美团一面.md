---
title: "Shio美团一面"
---

# Shio 美团一面

> 根据图片内容整理，以下为一面题目清单。

## 操作系统与 I/O

1. 进程的调度方法有哪些？不同调度方法分别适合什么场景？
2. 线程调度的开销主要由哪几部分组成？
3. 操作系统的 I/O 模型有哪些？
4. 讲一下阻塞 I/O 和非阻塞 I/O。
5. 为什么不能一下开上万个线程？

## 计算机网络

1. TCP 有哪些保证可靠传输的机制？
2. 流量控制和拥塞控制的区别是什么？

## MySQL

1. 为什么 MySQL 使用 B+ 树？
2. 索引有哪些类型？覆盖索引是什么？
3. 联合索引 `abc` 查询 `bc` 能不能走索引？
4. 索引失效的常见情况有哪些？
5. 怎么看一条 SQL 的执行计划？需要重点关注哪些字段？
6. MySQL 有哪些事务隔离级别？
7. 事务的四个特性是什么？分别依靠什么机制实现？
8. 什么是 MVCC？它是如何实现的？
9. 可重复读隔离级别是如何处理幻读的？
10. MySQL 加了哪些锁？只有行锁吗？

## LLM 与微调

1. LLM 的幻觉是什么？
2. 怎么解决或者缓解幻觉？
3. 除了 RAG 还有哪些方案？
4. 全量微调大概是在做什么？
5. LoRA 微调大概是在做什么？

> 备注：这部分当时没答上来，面试官提到了“微调”方向。

## RAG 项目

1. RAG 项目拷打。

## AI Coding 场景题

给了一段几行代码，内容是对所有 `user_ids` 执行一条 `update`。

1. 这个实现的风险点是什么？
2. 如果 `user_ids` 是百万级，并且更新的值是负数，会怎么样？
3. 怎么把它做成一个更工程化的版本？

## 参考答案（AI 生成）

> 以下答案由 AI 生成，仅供面试复盘参考。

### 1. 进程的调度方法有哪些？不同调度方法分别适合什么场景？

答：进程是资源分配单位，线程是 CPU 调度单位，协程是用户态轻量执行单元。进程隔离强，线程共享进程地址空间，协程切换成本更低。

### 2. 线程调度的开销主要由哪几部分组成？

答：进程是资源分配单位，线程是 CPU 调度单位，协程是用户态轻量执行单元。进程隔离强，线程共享进程地址空间，协程切换成本更低。

### 3. 操作系统的 I/O 模型有哪些？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 4. 讲一下阻塞 I/O 和非阻塞 I/O。

答：I/O 模型包括阻塞、非阻塞、I/O 多路复用、信号驱动和异步 I/O。服务端常用多路复用提升大量连接下的资源利用率。

### 5. 为什么不能一下开上万个线程？

答：进程是资源分配单位，线程是 CPU 调度单位，协程是用户态轻量执行单元。进程隔离强，线程共享进程地址空间，协程切换成本更低。

### 6. TCP 有哪些保证可靠传输的机制？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 7. 流量控制和拥塞控制的区别是什么？

答：TCP 拥塞控制包括慢启动、拥塞避免、快速重传和快速恢复。核心是根据丢包、重复 ACK 和 RTT 等信号调整拥塞窗口，控制网络中的在途数据量。

### 8. 为什么 MySQL 使用 B+ 树？

答：InnoDB 主键索引是聚簇索引，叶子节点保存整行；二级索引叶子节点保存主键值，查询其他列需要回表。覆盖索引能直接从索引返回结果。

### 9. 索引有哪些类型？覆盖索引是什么？

答：InnoDB 主键索引是聚簇索引，叶子节点保存整行；二级索引叶子节点保存主键值，查询其他列需要回表。覆盖索引能直接从索引返回结果。

### 10. 联合索引 abc 查询 bc 能不能走索引？

答：InnoDB 主键索引是聚簇索引，叶子节点保存整行；二级索引叶子节点保存主键值，查询其他列需要回表。覆盖索引能直接从索引返回结果。

### 11. 索引失效的常见情况有哪些？

答：InnoDB 主键索引是聚簇索引，叶子节点保存整行；二级索引叶子节点保存主键值，查询其他列需要回表。覆盖索引能直接从索引返回结果。

### 12. 怎么看一条 SQL 的执行计划？需要重点关注哪些字段？

答：看 EXPLAIN 时重点关注 type、key、rows、filtered、Extra。type 越接近 const/ref/range 越好，rows 越小越好，Extra 中关注 Using index、Using temporary、Using filesort。

### 13. MySQL 有哪些事务隔离级别？

答：MySQL 常见锁有行锁、表锁、间隙锁、临键锁和意向锁。隔离级别越高，并发冲突控制越强，InnoDB 通过 MVCC 和锁共同保证一致性。

### 14. 事务的四个特性是什么？分别依靠什么机制实现？

答：MySQL 回答要围绕表结构、索引、事务、锁、日志和执行计划展开。场景题优先说明查询路径、并发风险和可观测指标。

### 15. 什么是 MVCC？它是如何实现的？

答：MySQL 回答要围绕表结构、索引、事务、锁、日志和执行计划展开。场景题优先说明查询路径、并发风险和可观测指标。

### 16. 可重复读隔离级别是如何处理幻读的？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 17. MySQL 加了哪些锁？只有行锁吗？

答：MySQL 常见锁有行锁、表锁、间隙锁、临键锁和意向锁。隔离级别越高，并发冲突控制越强，InnoDB 通过 MVCC 和锁共同保证一致性。

### 18. LLM 的幻觉是什么？

答：AI 工程题可以按“模型能力、工具调用、上下文管理、评测、成本和安全”展开。结合实际工作流说明如何提升开发效率。

### 19. 怎么解决或者缓解幻觉？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 20. 除了 RAG 还有哪些方案？

答：RAG 包括文档解析、切分、embedding、向量检索、重排、Prompt 构造和模型生成。核心是提升知识可控性、降低幻觉并保留引用来源。

### 21. 全量微调大概是在做什么？

答：全量微调更新模型全部参数，成本高但能力改动大；LoRA 通过低秩适配矩阵训练少量参数，成本低，适合特定领域适配。

### 22. LoRA 微调大概是在做什么？

答：全量微调更新模型全部参数，成本高但能力改动大；LoRA 通过低秩适配矩阵训练少量参数，成本低，适合特定领域适配。

### 23. RAG 项目拷打。

答：RAG 包括文档解析、切分、embedding、向量检索、重排、Prompt 构造和模型生成。核心是提升知识可控性、降低幻觉并保留引用来源。

### 24. 这个实现的风险点是什么？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。

### 25. 如果 user_ids 是百万级，并且更新的值是负数，会怎么样？

答：游戏服务端题重点是状态同步、命中判定、延迟补偿、分区分服和反作弊。实时体验通常由客户端预测提升流畅度，服务器做权威校验。

### 26. 怎么把它做成一个更工程化的版本？

答：这题可以按“定义、核心机制、项目场景、风险和优化”展开。先给结论，再结合自己的项目或实践说明，最后补充边界条件和排查方法。
